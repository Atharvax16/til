torch>=2.1
torchvision>=0.16
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pillow>=10.0
jupyter>=1.0
ipykernel>=6.0

# Needed for the real frozen BioMedCLIP encoder. Without these the notebook falls back to a
# stub encoder and says so (see section 2).
open_clip_torch>=2.23
transformers>=4.30
